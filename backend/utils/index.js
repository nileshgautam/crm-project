const logActivity = require("./logActivity");
const response = require("./response");
const logger = require("./logger");

module.exports = {
  logActivity,
  response,
  logger,
};
// This module exports two utility functions: ActivityLog and response.
